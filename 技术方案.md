# 记账App技术方案

## 1. 模块设计与架构

### 后端模块架构（Python）
```
backend/
├── app/
│   ├── core/           # 核心配置和工具
│   ├── models/         # 数据模型
│   ├── schemas/        # Pydantic模式
│   ├── services/       # 业务逻辑层
│   ├── api/           # API路由
│   │   ├── v1/
│   │   │   ├── auth/      # 用户认证
│   │   │   ├── accounts/  # 账本管理
│   │   │   ├── bills/     # 账单管理
│   │   │   ├── assets/    # 资产管理
│   │   │   ├── budgets/   # 预算管理
│   │   │   ├── reports/   # 报表统计
│   │   │   ├── ai/        # AI对话
│   │   │   └── uploads/   # 文件上传
│   ├── utils/         # 工具函数
│   └── main.py        # 应用入口
```

### 前端模块架构
```
frontend/
├── src/
│   ├── pages/         # 页面组件
│   │   ├── home/         # 首页记账
│   │   ├── assets/       # 资产管理
│   │   ├── reports/      # 报表统计
│   │   └── ai/          # AI对话
│   ├── components/    # 通用组件
│   ├── services/      # API服务
│   ├── utils/         # 工具函数
│   └── stores/        # 状态管理
```

## 2. 服务架构设计

### 技术栈选择
**后端：**
- Framework: FastAPI (异步高性能)
- Database: PostgreSQL + Redis
- ORM: SQLAlchemy 2.0
- Authentication: JWT
- Task Queue: Celery
- File Storage: MinIO/AWS S3
- AI Integration: Dify API

**前端：**
- Mobile: Flutter/React Native
- Desktop: Electron + React
- State Management: Provider/Redux
- HTTP Client: Dio/Axios

### 部署架构
```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   Client App    │────│   Load Balancer  │────│  FastAPI Server │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                                                        │
                       ┌─────────────────┐             │
                       │     Redis       │─────────────┤
                       └─────────────────┘             │
                                                        │
                       ┌─────────────────┐             │
                       │   PostgreSQL    │─────────────┤
                       └─────────────────┘             │
                                                        │
                       ┌─────────────────┐             │
                       │   File Storage  │─────────────┘
                       └─────────────────┘
```

## 3. 数据库实体设计

### 核心实体模型

```python
# 用户表
class User:
    id: UUID
    username: str
    email: str
    password_hash: str
    avatar_url: str
    created_at: datetime
    updated_at: datetime

# 账本表
class Account:
    id: UUID
    user_id: UUID
    name: str
    description: str
    currency: str
    is_shared: bool
    members: List[UUID]  # 多人协作成员
    created_at: datetime

# 资产表
class Asset:
    id: UUID
    user_id: UUID
    name: str
    type: str  # 'bank_account', 'credit_card', 'cash', 'investment', 'property'
    balance: Decimal
    currency: str
    include_in_total: bool
    notes: str
    created_at: datetime

# 分类表
class Category:
    id: UUID
    user_id: UUID
    name: str
    type: str  # 'income', 'expense', 'transfer'
    icon: str
    color: str
    parent_id: UUID  # 支持子分类

# 账单表
class Bill:
    id: UUID
    user_id: UUID
    account_id: UUID
    asset_id: UUID
    category_id: UUID
    amount: Decimal
    currency: str
    type: str  # 'income', 'expense', 'transfer'
    description: str
    date: datetime
    created_at: datetime

# 预算表
class Budget:
    id: UUID
    user_id: UUID
    account_id: UUID
    name: str
    total_amount: Decimal
    period_type: str  # 'monthly', 'yearly'
    start_date: datetime
    end_date: datetime

# 预算分类明细表
class BudgetCategory:
    id: UUID
    budget_id: UUID
    category_id: UUID
    allocated_amount: Decimal
    spent_amount: Decimal

# 周期账单表（订阅、分期）
class RecurringBill:
    id: UUID
    user_id: UUID
    template_bill_id: UUID
    frequency: str  # 'daily', 'weekly', 'monthly', 'yearly'
    next_due_date: datetime
    end_date: datetime
    is_active: bool

# 债务表
class Debt:
    id: UUID
    user_id: UUID
    type: str  # 'borrow_in', 'lend_out'
    counterpart: str  # 对方姓名
    amount: Decimal
    currency: str
    description: str
    due_date: datetime
    is_settled: bool
```

### 数据库关系图
```
User ──┬── Account ── Bill
       ├── Asset
       ├── Category ── BudgetCategory
       ├── Budget ──── BudgetCategory
       ├── RecurringBill
       └── Debt
```

## 4. RESTful API接口设计

### 认证相关
```
POST /api/v1/auth/register     # 用户注册
POST /api/v1/auth/login        # 用户登录
POST /api/v1/auth/refresh      # 刷新token
POST /api/v1/auth/logout       # 用户登出
```

### 账本管理
```
GET    /api/v1/accounts              # 获取账本列表
POST   /api/v1/accounts              # 创建账本
GET    /api/v1/accounts/{id}         # 获取账本详情
PUT    /api/v1/accounts/{id}         # 更新账本
DELETE /api/v1/accounts/{id}         # 删除账本
GET    /api/v1/accounts/{id}/summary # 账本汇总（收入/支出/结余）
```

### 账单管理
```
GET    /api/v1/bills                 # 获取账单列表
POST   /api/v1/bills                 # 创建账单
GET    /api/v1/bills/{id}            # 获取账单详情
PUT    /api/v1/bills/{id}            # 更新账单
DELETE /api/v1/bills/{id}            # 删除账单
GET    /api/v1/bills/daily-summary   # 按天聚合账单
POST   /api/v1/bills/batch-import    # 批量导入账单
```

### 资产管理
```
GET    /api/v1/assets                # 获取资产列表
POST   /api/v1/assets                # 创建资产
GET    /api/v1/assets/{id}           # 获取资产详情
PUT    /api/v1/assets/{id}           # 更新资产
DELETE /api/v1/assets/{id}           # 删除资产
GET    /api/v1/assets/overview       # 资产总览
GET    /api/v1/assets/trends         # 资产趋势
```

### 预算管理
```
GET    /api/v1/budgets               # 获取预算列表
POST   /api/v1/budgets               # 创建预算
GET    /api/v1/budgets/{id}          # 获取预算详情
PUT    /api/v1/budgets/{id}          # 更新预算
DELETE /api/v1/budgets/{id}          # 删除预算
GET    /api/v1/budgets/{id}/progress # 预算执行进度
```

### 报表统计
```
GET /api/v1/reports/summary          # 收支汇总
GET /api/v1/reports/trends           # 趋势分析
GET /api/v1/reports/categories       # 分类统计
GET /api/v1/reports/comparison       # 对比分析
```

### AI对话
```
POST /api/v1/ai/chat                 # AI对话
GET  /api/v1/ai/suggestions          # 智能建议
```

### 文件上传
```
POST /api/v1/uploads/image           # 上传图片
POST /api/v1/uploads/voice           # 上传语音
POST /api/v1/uploads/csv             # 上传CSV文件
```

## 5. 前后端交互设计

### 数据流架构
```
Client App ←→ HTTP/HTTPS ←→ FastAPI Server ←→ PostgreSQL
    ↓                            ↓
WebSocket                    Redis Cache
(实时更新)                   (会话/缓存)
```

### 前端状态管理
```javascript
// 全局状态结构
{
  auth: {
    user: User,
    token: string,
    isAuthenticated: boolean
  },
  accounts: {
    current: Account,
    list: Account[],
    summary: AccountSummary
  },
  bills: {
    list: Bill[],
    dailySummary: DailySummary[],
    filters: BillFilters
  },
  assets: {
    list: Asset[],
    overview: AssetOverview,
    trends: AssetTrend[]
  },
  budgets: {
    list: Budget[],
    current: Budget,
    progress: BudgetProgress
  }
}
```

### API响应格式
```javascript
// 统一响应格式
{
  "success": true,
  "data": {},
  "message": "操作成功",
  "code": 200,
  "timestamp": "2024-01-01T00:00:00Z"
}

// 分页响应格式
{
  "success": true,
  "data": {
    "items": [],
    "total": 100,
    "page": 1,
    "size": 20,
    "has_more": true
  }
}
```

### 实时更新机制
- WebSocket连接用于实时账单更新
- 多人协作账本的实时同步
- 预算超支提醒

## 6. 测试方案设计

### 后端测试策略

**单元测试 (pytest)**
```python
# 测试覆盖率目标：80%+
tests/
├── unit/
│   ├── test_models.py      # 数据模型测试
│   ├── test_services.py    # 业务逻辑测试
│   ├── test_utils.py       # 工具函数测试
│   └── test_auth.py        # 认证功能测试
├── integration/
│   ├── test_api.py         # API集成测试
│   ├── test_database.py    # 数据库集成测试
│   └── test_external.py    # 外部服务测试
└── e2e/
    └── test_workflows.py    # 端到端测试
```

**API测试 (pytest + httpx)**
- 所有API端点的功能测试
- 权限验证测试
- 数据验证测试
- 错误处理测试

**性能测试 (locust)**
- 并发用户负载测试
- API响应时间测试
- 数据库查询性能测试

### 前端测试策略

**单元测试**
- 组件渲染测试
- 状态管理测试
- 工具函数测试

**集成测试**
- API调用测试
- 页面流程测试
- 跨平台兼容性测试

**E2E测试**
- 关键用户流程测试
- 记账完整流程测试
- 多设备同步测试

### CI/CD流程
```yaml
# GitHub Actions / GitLab CI
stages:
  - lint          # 代码规范检查
  - test          # 自动化测试
  - build         # 构建应用
  - deploy        # 部署到环境
  - monitor       # 监控告警
```

## 7. 技术实现要点

### 安全设计
- JWT Token认证 + Refresh Token机制
- API接口权限控制中间件
- 敏感数据加密存储
- SQL注入防护
- 文件上传安全验证

### 性能优化
- Redis缓存热点数据
- 数据库索引优化
- API响应分页机制
- 图片压缩和CDN
- 前端组件懒加载

### 国际化支持
- 多币种转换
- 实时汇率API
- 多语言界面
- 本地化日期格式

### 监控告警
- 应用性能监控（APM）
- 错误日志收集
- 系统资源监控
- 业务指标统计

## 8. 开发计划

### 第一阶段（MVP）
1. 用户认证系统
2. 基础账单CRUD
3. 简单资产管理
4. 基础报表功能

### 第二阶段（功能完善）
1. 预算管理系统
2. 多账本协作
3. 图片/语音识别
4. 数据导入导出

### 第三阶段（高级功能）
1. AI对话功能
2. 高级报表分析
3. 周期账单
4. 移动端优化

### 第四阶段（运营优化）
1. 性能优化
2. 用户体验优化
3. 数据分析
4. 运维监控
